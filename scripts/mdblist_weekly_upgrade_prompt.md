你现在要生成一篇适合 Astro Paper 发布的中文 Markdown 博客正文。

目标栏目：每周影视推荐。

硬性要求：
1. 标题定位改成“每周影视推荐”，不要在正文和读者可见内容中出现 MDBList 这个信息源名称。
2. 保留电影 + 剧集两类内容，不要只写剧集。
3. 保留文章头图，但正文中每个作品也都必须显示自己的海报，直接使用上下文里给出的 `poster` URL。
4. 每个作品都必须包含以下结构：
   - `### 作品名`
   - 海报图片 Markdown
   - `#### 基本信息`
   - `#### 剧情概要`
   - `#### 推荐理由`
   - `#### 评论总结`
5. 作品标题显示规则：
   - 所有作品统一使用 `中文名（英文名）` 作为 `###` 标题格式。
   - 即使中文名已经非常稳定，也依然保留英文名括注。
   - 不要只显示英文片名/剧名，也不要只显示纯中文名。
6. 基本信息显示规则：
   - `类型` 必须翻译成中文，不要保留英文 genre。
   - 只显示 `IMDb 评分`，不要显示 `豆瓣评分`。
7. 评论总结要求：
   - 只基于我提供给你的 REVIEW CONTEXT 里的真实评论页/详情页文本来做轻量归纳；每部作品 2~3 句。
   - 不是简单复述分数，要概括评论者最常提到的优点、争议点或观看门槛。
   - 如果某部作品在 REVIEW CONTEXT 中没有足够评论内容，就写得更克制，但不要写“从可见讨论线索看”“评论信号不足”等元话术，更不要编造。
8. 全文结构必须只有：
   - `## 电影推荐`
   - `## 剧集推荐`
9. 不要输出开头导语。
10. 不要输出 `## 本周推荐看点`。
11. 不要输出 `## 本周口碑观察`。
12. 不要输出 frontmatter，不要加代码围栏，不要解释自己做了什么。
13. 读者可见正文中不要写“数据来源：MDBList 官方热门榜单”。
14. 可以把上下文中的评分、简介、海报、豆瓣链接等当作底稿，但你需要自己联网补充 IMDb / 豆瓣评论信号后再写成文章。

执行要求：
- 先阅读我提供的上下文，每个作品都有结构化底稿字段。
- 对每个作品，尽量联网查找 IMDb / 豆瓣对应页面中的评论信号或常见评价关键词，再生成 `评论总结`。
- 如果评论页面采集受限，允许改用评分页、短评页、媒体摘要页里的稳定可见评论线索，但不要编造具体评论内容。
- 最终把完整 Markdown 正文写到：
  /tmp/mdblist_weekly_ai_output.md
- 写完后再读回这个文件确认内容存在。
- 你的最终回答必须只输出正文 Markdown 本身。
