# 任务

你要把本期海外科技访谈/深度讨论播客，写成一篇中文长文博客笔记。

- 任务：{task}
- 日期：{date}

# 本期素材

本期播客的完整音频已作为附件随本请求直接提供，**音频是正文内容的唯一来源**：请直接依据音频写作。下面的元数据只用于填写 `### 基本信息`（节目名、嘉宾、日期、来源、链接），不要把元数据当成内容证据来扩写观点。

```md
{source_text}
```

# 输出要求

只输出可归档的 Markdown 正文，不要输出 frontmatter，不要输出代码围栏。

文章结构要求（只写本期这一档播客，不要写成多期合辑）：

1. 第一行直接输出本期播客的小节标题 `## 中文播客标题`，不要在它之前写任何总览、清单或引言。
2. 小节标题之后按以下顺序展开：
   - `### 中文主题`
   - `### 基本信息`
     - `- **节目**：...`
     - `- **嘉宾**：...`，输入没有明确嘉宾时写“未标明”，不要猜。
     - `- **日期**：...`
     - `- **来源**：...`
     - `- **链接**：...`
   - `### 核心观点`
     - 提炼本期播客最核心的观点或判断，点出节目真正想表达的立场，而不是流程式概述。
   - `### Highlights`
     - 3-5 条 bullet，每条必须是可由本期音频内容支持的技术、产品、职业或产业判断。
   - `### 长文笔记`
     - 围绕播客内容；提炼知识、观点、依据或启发，不能只描述“聊了什么”。

# 标题翻译要求

- 本期播客的 `##` 小节标题必须翻译成自然中文，不能直接使用英文原题作为标题。
- 标题要忠实表达本期主题，不要标题党化，不要扩写成额外观点。
- 专有名词、产品名、公司名、人名、模型名和节目名可以保留英文，例如 OpenAI、Claude、NVIDIA、Latent Space。
- 原始节目名、来源和链接继续放在 `### 基本信息` 内；不要因为标题翻译而改动元数据。

# 质量要求

- 必须是长文中文笔记，不是短消息摘要；要写到“不听播客也能理解本期具体内容”的程度。
- 长文笔记的目标是把音频内容提炼成知识和观点，而不是单纯描述访谈流程。优先写“这个观点为什么重要、背后的机制是什么、哪些例子支撑它、它对读者判断有什么帮助”。
- 重点写访谈里的技术、产品、职业、产业判断和可复盘观点；不要为了简短压缩信息，不限制展开长度。
- `### 长文笔记` 应尽量讲清：这期主要问题、问题背景、核心观点、观点理由、例子/事件/产品/人物/公司/技术背景、争议/风险/不确定性、对开发者/创业者/产品/AI 行业或普通读者的启发。