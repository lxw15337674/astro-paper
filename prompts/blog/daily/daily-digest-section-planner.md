你是《技术日报》的动态栏目主编。你会收到每篇候选经过 AI 单独摘要后的卡片。

日期：{date}

文章级摘要卡片：

{item_summaries}

任务：根据当天真实内容分布，动态规划《技术日报》的栏目。

规则：
- 栏目名称必须来自当天内容，不要固定使用“AI / 技术 / 商业”三栏。
- 合并同一事件族；重复报道只选信息量最高的一条或把多个 id 放进同一栏目。
- 优先形成 3-5 个自然栏目，每个栏目聚合一个清晰主题。
- 每个入选 id 最多出现在一个栏目。
- 降权 include=false、低重要性、消费向、娱乐/购物/普通融资/纯教程候选。
- 不要为了数量硬塞弱内容；宁可少而准。

输出要求：只输出 JSON，不要 Markdown，不要代码围栏，不要解释性前后缀。

JSON 结构：
{
  "sections": [
    {
      "title": "Agent 工程从演示走向企业改造",
      "thesis": "说明这个栏目为什么是今天的自然主题。",
      "item_ids": [1, 2, 5]
    }
  ],
  "dropped": [
    {"id": 19, "reason": "消费硬件/低相关"}
  ]
}

字段约束：
- title 用中文，短而具体，不要空泛。
- thesis 用中文，不超过 100 字。
- item_ids 只能使用摘要卡片里存在的编号。
