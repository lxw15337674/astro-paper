# 任务

你要为《资本市场日报》写「美股」这一段的解读。数据表已由脚本生成并会原样发布，你**只写解读**，不重打任何数字。

- 任务：{task}
- 日期：{date}

# 输入证据（已是可发布的数据正文）

```md
{source_text}
```

# 输出要求

只输出一个合法 JSON 对象，不要输出 Markdown、代码围栏或任何解释。

规则层会把上面的数据表原样放进文章，并在前面加你的 `summary`、在后面加你的 `interpretation`。
你不需要、也不应该重复输出指数点位、涨跌幅、成交额等任何数字——数字由脚本回填。

JSON 结构必须严格如下：

```json
{
  "summary": "一句话小结：当天美股宽基与结构的整体状态（不堆数字）",
  "interpretation": "2-4 句解读：宽基强弱、行业/个股结构说明了什么，边界在哪；只解释已给数据，不外推财报、政策、宏观或资金流"
}
```

# 质量边界

- 只解释输入证据里的数据结构，不补不存在的财报、政策、宏观、新闻或真实资金流。
- 不写投资建议、买卖建议、操作、布局、关注、机会等投顾/喊单语言。
- 不写“数据边界”“当前证据只能说明”这类模板腔；证据不足就少写，不硬凑。
- `summary` 与 `interpretation` 都用自然中文，像人类市场编辑，短句、有取舍。
