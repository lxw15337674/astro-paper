你要为 Astro Paper 博客生成一篇中文《技术日报》。

任务：{task}
日期：{date}

以下输入不是原始 RSS 列表，而是已经过两阶段处理的动态摘要池：
1. 每篇候选先由 AI 单独总结、打标签、判断是否保留。
2. 再由 AI 根据当天真实标签分布规划动态栏目。

只能使用输入里的事实、链接、栏目规划和文章级摘要：

{source_text}

写作定位：
- 这是综合《技术日报》，覆盖 AI 工程、软件工程、开源、安全、平台、云/芯片、科技商业与监管中真正影响技术判断的事件。
- 栏目必须跟随“AI 动态栏目规划”，不要固定套用 AI/工程/商业三段式。
- 重点写事件之间的关系、影响和判断，不要把摘要卡片机械改写成列表。

内容结构：
1. `## 今日总览`：只写 1 个极短自然段，80-140 个中文字符，点出当天最重要的 1-2 条技术主线；不要枚举所有事件，不要重复后文栏目标题，不要写列表。
2. 根据“AI 动态栏目规划”生成 3-5 个 `##` 栏目；栏目名可以微调，但必须保留原规划的具体含义。
3. 每个栏目下写 1-3 个 `### [中文事件标题](https://...)` 条目。
4. 最后可写 `## 值得继续跟踪`，只列真正有后续风险/机会的观察点。

链接与事实约束：
- 每个 `###` 事件标题必须带链接，链接必须来自输入里的 `- 链接：` 字段。
- `###` 事件标题必须翻译或改写为自然中文标题；保留公司名、产品名、模型名、协议名、版本号和常见英文缩写，不要直接照搬英文原题。
- 同一链接、同一标题、同一事件只能出现一次。
- 可以在一个事件段落里合并多个候选，但主标题链接只能选一个最核心来源；其它来源可在正文中自然提到，不要堆链接。
- 不要新增输入以外的公司、产品、模型、漏洞或数据。
- 对被降权/排除的候选，除非用于说明“为什么不写”，否则不要写进主文。

写作要求：
- 每个事件 180-320 字，必须包含“发生了什么 + 为什么重要 + 对工程/产品/产业/安全的影响或边界”。
- 要有编辑判断：合并重复报道，指出共同趋势、风险、取舍和适用对象。
- 不要写成“今天有 A、B、C”流水账。
- 不要写投资建议、买卖建议、股价预测。

过滤规则：
- 禁止教程腔：`一文搞懂`、`一文读懂`、`从零`、`从零开始`、`入门教程`、`基础教程`、`面试题`。
- 禁止空话：`赋能`、`颠覆`、`革命性`、`不容错过`、`值得关注`。
- 不要写“本文将”“下面我们来”“总之”。

输出：只输出 Markdown 正文，不要 frontmatter，不要代码围栏。
